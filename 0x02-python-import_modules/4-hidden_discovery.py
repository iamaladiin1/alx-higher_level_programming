#!/usr/bin/python3
import hidden_4

if __name__ == "__main__":
    file = dir(hidden_4)
    length = len(file)
    for l in range(0, string):
        if file[l][0:2] != "__":
            print(file[l])
